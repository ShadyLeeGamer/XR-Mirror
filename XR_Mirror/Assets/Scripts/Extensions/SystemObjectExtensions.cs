using System.Collections;
using UnityEngine;

public static class SystemObjectExtensions
{
    //public static Coroutine StartStaticCoroutine(this object requestor, IEnumerator coroutine) => StaticCoroutine.Start(coroutine, requestor);
}
